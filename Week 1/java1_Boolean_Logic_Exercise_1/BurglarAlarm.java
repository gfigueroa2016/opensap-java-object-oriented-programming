class BurglarAlarm {

	int alarmLevel = 1;

	boolean beep() {
		if (alarmLevel > 2) {
		    // Add a return statment with an appropriate value to the next line
		    return true;
            
		} else {
		    // Add a return statment with an appropriate value to the next line
		    return false;
            
    	}
    }
}
